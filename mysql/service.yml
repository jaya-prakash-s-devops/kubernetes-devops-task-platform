apiVersion: v1
kind: Service

metadata:
  name: mysql
  namespace: devops-platform
  labels:
    app: mysql

spec:
  selector:
    app: mysql

  ports:
    - name: mysql
      port: 3306
      targetPort: 3306

    - name: metrics
      port: 9104
      targetPort: 9104

  type: ClusterIP